# 搜同破解 MVP 1.0 rc22 验证结果（复核版）

复核日期：2026-08-08  
对象：`index.html`、`operations-prototype.html`  
结论：原型可继续运营评审，但 P0 未达到全量验收通过；详见 `RC22_LOGIC_AUDIT_2026-08-08.md`。

## 1. 已取得的证据

| 检查项 | 结论 | 证据等级 | 说明 |
|---|---|---:|---|
| 主脚本语法 | 通过 | L1 | 主脚本通过 `node --check` |
| 动作处理器映射 | 通过 | L1 | 用于发现漏绑事件；不等于业务闭环 |
| 启动流程与广告队列 | 通过 | L2 | 年龄确认、5秒倒计时、手动跳过、6个广告、平台公告已点击验证 |
| 短视频移动布局 | 通过 | L2 | 修复 390×844 主体高度为0；作者、操作区和底栏均在视口内 |
| 短视频关注/收藏 | 通过（本地） | L2/L3 | 关注作者后关注流从空态变为1条；收藏后按钮变为已收藏并可进入收藏记录 |
| 标签排序 | 通过（本地） | L1/L2 | 推荐、最新、最热映射为不同顺序 |
| AI结果删除 | 通过（代码与状态） | L3 | 只删除用户侧结果，成功任务和账本保留 |
| AI异常退回 | 通过（本地） | L3 | 任务状态与资产状态分离，退回由 `assetReturned` 防重 |
| AI任务与订单联动 | 通过（本地） | L3 | 创建任务生成同业务单号的处理中订单；成功变成功、异常变失败且记录退回原因 |
| 钱包流水前后余额 | 通过（本地） | L2/L3 | 页面展示资产类型、业务单号、变动量及 `before → after`；不可覆盖性仍需服务端证明 |
| 合集收藏 | 通过（本地） | L2/L3 | 合集详情可收藏；收藏记录按合集类型展示并返回合集详情 |
| 退款冲正流水 | 通过（本地） | L3 | 会员、充值、内容、合集的资产冲正均追加流水 |
| 设备/注销保护 | 通过（本地） | L2/L3 | 设备退出二次确认；注销校验订单、冻结资产、未结工单并再次确认 |

## 2. 只能判定为“部分”的项目

| 检查项 | 结论 | 未覆盖内容 |
|---|---|---|
| 控制台 | 代表路径通过 | 尚未遍历所有页面、状态和130个动作；不能写“全站0错误” |
| 标签与合集 | 部分通过 | 默认路径可达；对象下架、价格变化和服务端分页未联调 |
| 充值恢复原意图 | 部分通过 | 本地路径支持过期、取消、价格变化确认；真实支付回跳/查单未联调 |
| 订单状态 | 部分通过 | 成功与退款可动态变化；失败、关闭、拒付主要为样例数据 |
| 会员规则 | 部分通过 | 同族顺延、不同族并存、当前周期发放可演示；周期任务、三重限购和到期补偿未证明 |
| AI任务 | 部分通过 | 本地状态机、失败退回、新ID重生可演示；真实编排、审核和推送未联调 |
| 评论 | 部分通过 | 回复、点赞、举报、审核中、失败保留草稿可演示；409状态变化和真实审核回调未联调 |
| 消息/客服 | 部分通过 | 分类、已读、部分对象直达和工单创建可演示；可靠投递与SLA未联调 |
| 全部独立页状态 | 未完成 | 多数页面未逐页覆盖默认、加载、空、失败、未登录、无权限和状态变化 |
| 多视口 | 部分通过 | 390×844、430×932 的短视频首屏已复测；全部二/三级页仍需逐页视觉回归 |

## 3. 浏览器复核路径

1. 年龄确认 → 资源加载 → 5秒启动广告 → 手动跳过 → 首页广告队列 → 平台公告。
2. 390×844 短视频推荐 → 登录 → 关注作者 → 关注流出现该作者内容。
3. 短视频收藏 → 状态变为已收藏；“我的收藏”专用入口已修正为收藏 Tab。
4. AI任务创建 → 订单为处理中 → 成功后订单同步成功；异常任务订单同步失败并原路退回冻结资产。
5. 合集详情收藏 → 我的收藏显示“合集” → 点击返回合集详情；钱包流水显示变动前后余额。
6. 其余评论、设备与注销为本地状态级检查；不能替代真实后台联调。

## 4. 截图证据

- `output/playwright/audit-short-current-390.png`：修复前缺陷证据。
- `output/playwright/audit-short-fixed-390.png`：修复后移动首屏。
- `output/playwright/audit-short-fixed-430.png`：430×932 修复后首屏。
- 既有 rc22 截图只证明对应画面，不证明未截图的状态或全部业务分支。

## 5. 上线前仍需完成

- 服务端支付回调、账本唯一约束、会员周期发放、风控资格、推送、审核回调、分页和注销任务联调。
- 按页面矩阵逐页执行七类状态和 390×844、430×932 视觉回归。
- 关闭 PRD O-01 至 O-15；特别是 AI 取整、首次资格、同卡定义、注销规则、VIP去广告范围和单集抵扣合集价。
- 补齐全局搜索组合筛选、合集更新订阅、观看记录单条删除、评论删除/申诉/日志和核心埋点验收。
