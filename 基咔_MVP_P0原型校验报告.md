# 基咔 MVP P0 原型校验报告

校验日期：2026-08-15  
需求基线：`基咔_MVP_P0全功能清单_双重核对版.md` v1.1  
原型基线：`operations-prototype.html`

## 一、结论

当前原型的核心框架、主要二三级页面和大部分业务闭环已经具备，但**尚不建议冻结为MVP 1.0研发基线**。

- 阻断问题：4项。
- 超出MVP或旧口径残留：4项。
- 页面入口与信息展示待调整：3项。
- 脚本静态语法：通过。
- 浏览器交互回归：本次未执行；本地 `file://` 页面受应用浏览器控制策略限制。

## 二、P0阻断问题

### P0-01 社区缺少帖子点赞闭环

需求要求社区支持浏览、评论、点赞、收藏、关注、举报和拉黑。

当前情况：

- 交流帖子卡片仅有浏览、求片动作、评论、更多，缺少点赞。
- 资源帖子卡片仅有作者时间、浏览和评论，缺少点赞与更多。
- 帖子详情仅有收藏、评论、举报，缺少点赞及点赞数。
- 评论点赞已实现，但不能替代帖子点赞。

验收要求：未登录点击点赞先登录；登录后可点赞/取消点赞；列表与详情数量同步；重复点击不重复累计。

### P0-02 栏目弹窗广告触发范围错误

需求要求每次冷启动后，短视频不弹；首次进入长视频、AI专区、社区或我的任一栏目时展示一次，关闭一个继续展示下一个，最多6个；同次启动不重复。

当前情况：

- 代码仅在进入长视频时触发。
- 控制器初始化和冷启动重置使用 `sectionCampaignShown`，实际判断却使用未定义的 `longCampaignShown`，状态字段不一致。
- AI专区、社区、我的首次进入均不会触发。

验收要求：统一使用一个“本次启动已展示”状态；四个非短视频栏目共享；后台切回不重置，重新冷启动才重置。

### P0-03 APP下载落地页不可用

当前情况：

- “下载App”链接为 `#`，点击无实际结果。
- 二维码为49个装饰方块，不可扫码。
- 缺少iOS/Android识别、下载失败提示和H5兜底。

验收要求：二维码和下载按钮至少进入可配置的真实地址；无法下载时给出明确提示并允许进入H5。

### P0-04 首次进入协议入口不可访问

18岁确认页中的“用户协议”“隐私政策”均为 `href="#"`。设置页虽然有协议入口，但不能替代首次确认前的可访问协议。

验收要求：首次确认前可分别查看用户协议、隐私政策和未成年人保护规则，并能返回确认页继续操作。

## 三、超出MVP或旧口径残留

### S-01 限时狂欢卡仍完整存在

清单明确“限时狂欢卡”为P1，不进入MVP 1.0。原型仍包含卡种、资格、购买、赠送资产、限购和首购引导，需要从MVP版隐藏。

### S-02 内容更新提醒仍存在

清单明确内容更新提醒不进入MVP 1.0。当前消息中心包含“收藏内容已更新”，资源帖文案也出现“更新后通过消息提醒”。MVP仅保留社区、订单、AI和系统等必要消息。

### S-03 长视频顶部频道口径未对齐

清单正文给出的可配置频道为“精选、最新、热门、合集、分类”；当前原型使用“关注、推荐、独家、发现、类型、性癖、厂商、地区”。截图菜单在清单中标注为待运营确认，不能直接作为已确认P0。

处理建议：冻结前由产品只保留一套正式频道配置，并在清单中写死。

### S-04 会员名称存在简称

卡种正式名称已确认是“AI专区年卡”，但会员切换标签仍显示“AI年卡”。建议全站统一正式名称。

## 四、页面入口与展示问题

### M-01 “我的购买”与“我的卡券”入口指向不准确

- “我的购买”当前进入订单中心，但需求中的已购内容应进入“我的内容-已购”。
- “我的卡券”当前进入会员中心，没有直接展示精品券余额、来源和适用说明。

### M-02 社区资源卡信息不完整

资源卡需要与交流帖子保持一致的可识别信息：头像、昵称、时间、帖子类型、标题、正文摘要、图片/视频、浏览、评论、点赞和更多。当前资源卡只有标题、话题、双图占位、作者时间、浏览和评论。

### M-03 社区发布状态存在口径冲突

双重核对清单要求新账号或含媒体帖子进入前置处理；此前产品决定又要求前台不出现“审核/处理中”，仅命中敏感词时直接失败。当前原型采用“处理中、仅自己可见”。

这不是实现遗漏，而是基线冲突。冻结前必须二选一并同步清单、原型、消息和“我的帖子”状态：

1. 保留前置处理：允许前台展示处理中状态。
2. 前台直接发布：命中敏感词失败，其余内容直接进入信息流，后台风控异步处置。

## 五、已通过的主要能力

- 五大底部入口、冷启动资源加载、5秒启动广告和18岁确认均已建立。
- 短视频三Tab、竖滑、默认播放、暂停、清屏、推广落地页、5秒VIP试看蒙层已建立。
- 长视频列表、分类、搜索、标签聚合、合集、逐集播放、续看、VIP/金币权益和播放器控制已建立。
- 会员、充值档位、付费金币/活动币/AI额度/精品券分账和订单状态已建立。
- AI四项工具、冻结、实扣、失败退回、任务记录、任务详情、结果页和重新生成已建立。
- 社区交流/资源、求片、图文视频帖子、帖子详情、大图/视频查看、评论回复、评论点赞、举报和发布入口已建立。
- 登录校验已覆盖收藏、关注、举报、评论、点赞、购买、充值、会员、AI任务、消息和设置等主要操作。
- 钱包、订单、观看记录、收藏、客服、设备管理、账号注销和更换APP图标已建立。
- 视频详情“为你推荐”位于评论上方，默认3行2列。
- 页面历史栈保存来源页和核心状态，视频详情进入作者页再返回的链路已有统一返回机制。

## 六、无法仅凭本原型验收

以下内容需要研发接口、后台或独立测试证据，不能用单文件前端原型判定通过：

- 支付回调、服务端查单、重复订单复用、退款、拒付和补单。
- 资产唯一流水、会员周期发放和10秒内权益到账。
- 内容、评论、AI结果、举报及申诉的真实处理链路。
- 运营、客服、财务、数据和风控权限隔离。
- 埋点唯一性、业务指标和关键异常监控。
- App真实更换桌面图标、设备限制、注销冷静期和跨端进度同步。

## 七、修复优先级

1. 补齐社区帖子点赞，并统一交流/资源/详情三处数据。
2. 修复非短视频栏目弹窗广告的单次冷启动触发逻辑。
3. 移除限时狂欢卡和内容更新提醒等P1残留。
4. 修复下载落地页及首次协议入口。
5. 确认社区发布状态和长视频顶部频道两项冲突口径。
6. 调整“我的购买”“我的卡券”和资源帖子卡的信息架构。

完成以上项目后，再进行390×844、430×932视口的真实点击回归和控制台零错误检查，方可冻结MVP 1.0原型。
